# Homebrew
eval "$(/opt/homebrew/bin/brew shellenv zsh)"

# Rust
export PATH="$HOME/.cargo/bin:$PATH"

# Local binaries
export PATH="$HOME/.local/bin:$PATH"

# Local overrides (not tracked in git)
[ -f ~/.zenv.local ] && source ~/.zenv.local
